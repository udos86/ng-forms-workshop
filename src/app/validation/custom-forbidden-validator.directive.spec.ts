import { CustomForbiddenValidatorDirective } from './custom-forbidden-validator.directive';

describe('CustomForbiddenValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomForbiddenValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
