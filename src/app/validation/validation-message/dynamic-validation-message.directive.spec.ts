import { DynamicValidationMessageDirective } from './dynamic-validation-message.directive';

describe('DynamicValidationMessageDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicValidationMessageDirective();
    expect(directive).toBeTruthy();
  });
});
